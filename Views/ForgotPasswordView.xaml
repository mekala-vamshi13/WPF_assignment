<Window x:Class="Assignment.Views.ForgotPasswordView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Assignment.Views"
        mc:Ignorable="d"
        Title="ForgotPasswordView" Height="450" Width="800">
    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Width="300" Height="275" Margin="247,54,246.333,91.667">

            <TextBlock Text="Email:" Margin="5" />
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Width="200" Height="30" Margin="5" />


            <TextBlock Text="Password:" Margin="5" />
            <PasswordBox PasswordChanged="PasswordBox_PasswordChanged" Width="200" Height="30" Margin="5" />

            <TextBlock Text="Confirm Password:" Margin="5" />
            <PasswordBox PasswordChanged="ConfirmPasswordBox_PasswordChanged" Width="200" Height="30" Margin="5" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="5" />
            <!-- Submit Button -->
            <Button Content="Submit" Width="100"  Command="{Binding SubmitCommand}" Height="30" Margin="5" HorizontalAlignment="Center"/>

        </StackPanel>

    </Grid>
</Window>
